version: '3.8'

services:
  radar-scraper:
    build: .
    container_name: radar_scraper
    working_dir: /app
    volumes:
      - ${FILE_PATH}:/app
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - TZ=America/Sao_Paulo
    restart: always
